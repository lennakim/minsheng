<p id="notice"><%= notice %></p>
Category: <% @category.ancestors.each do |category | %><%= category.name %><% end %> >> <%= @category.name %>
    <table class="table">
      <tr>
        <th>Title</th>
        <th>Phone</th>
        <th>Address</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

      <% @shops.each do |shop| %>
        <tr>
        <td><%= shop.title %></td>
        <td><%= shop.phone %></td>
        <td><%= shop.address %></td>
        <td><%= link_to 'Show', shop %></td>
      </tr>
    <% end %>
    </table>
