<h1>Listing shops</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Phone</th>
    <th>Address</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shops.each do |shop| %>
  <tr>
    <td><%= shop.title %></td>
    <td><%= shop.phone %></td>
    <td><%= shop.address %></td>
    <td><%= link_to 'Show', shop %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shop', new_shop_path %>
