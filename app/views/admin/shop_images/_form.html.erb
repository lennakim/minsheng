<%= form_for [:admin,@shop_image] do |f| %>
  <% if @shop_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop_image.errors.count, "error") %> prohibited this shop_image from being saved:</h2>

      <ul>
      <% @shop_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
